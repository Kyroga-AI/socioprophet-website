project_id = "socioprophet-website"

# Both are called anonymously from public website forms, so no Supabase JWT is sent.
[functions.submit-lead]
verify_jwt = false

[functions.join-waitlist]
verify_jwt = false
